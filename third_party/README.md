# third_party

git submodules — upstream benchmark and agent repos vendored in this tree.

```bash
git submodule update --init --recursive
```

see [recipe.md](../recipe.md) §4 for links and pip extras.
